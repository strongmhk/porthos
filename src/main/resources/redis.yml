spring:
  data:
    redis:
      # When running the app container, access host-published Redis via host gateway.
      host: ${SPRING_DATA_REDIS_HOST:host.docker.internal}
      port: ${SPRING_DATA_REDIS_PORT:16379}
      password: ${SPRING_DATA_REDIS_PASSWORD:nuntteo_qa_user1!}
      username: ${SPRING_DATA_REDIS_USERNAME:}
      ssl:
        enabled: ${SPRING_DATA_REDIS_SSL_ENABLED:false}
